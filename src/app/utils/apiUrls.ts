import environments from "./environments";

const apiUrls: { [key: string]: string } = {
  exercise1: `${environments.apiUrl}/api/exercise1`,
  exercise2: `${environments.apiUrl}/api/exercise2`,
};

export default apiUrls;
